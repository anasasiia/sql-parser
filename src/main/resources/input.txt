SELECT author.name, author.age
FROM * (FROM * A) a_alias
INNER JOIN libraries ON (author.id = libraries.author_id)
WHERE a BETWEEN 2000 AND 9000 AND b > 3 AND c NOT LIKE 'CCC'
GROUP BY a, b, c
ORDER BY a ASC, b DESC
OFFSET 10
LIMIT 5